# Codexveil · oss-gap 矩阵 + 装机就绪 · 2026-07-23

**MODE：** `oss-gap` · **WRITE_POLICY：** `local-commit`（本会话可补丁 + 本地 commit；**禁止 push / asar**）  
**REPO / WT：** `C:\Users\yuanjia\orca\workspaces\Codexveil\cv-oss-ready` · 支 `xvyimu/cv-oss-ready`  
**组合 tip 基线：** `136ed99`（`main` 同步）  
**公开性：** 公开 GitHub `xvyimu/Codexveil`（独立产品线 · ADR 0006 · 仅 `origin`）  
**STACK_SSOT：** `docs/PROJECT.md` §1.5（Windows 桌面侧车 · CDP Skin · Node≥20 ESM + pwsh）  
**OUT_OF_SCOPE：** asar 重打 · glass 默认开 · 未授权 push · 第二 injector · 主题 catalog 膨胀（产品 **arina-only**）

## 一句话

**L0 PASS**（无密钥进库；SECURITY 齐；`pnpm-lock` 可扫；本机 control.token 仅装态）。**L1 PASS**（LICENSE/README/CONTRIBUTING/SECURITY/CHANGELOG/身份卡齐；缺 CoC / `.editorconfig` → P2）。**L2 选修偏弱但可过：** CI `themes-gate` + frozen lockfile + PR 模板有；**缺 Dependabot / CI audit / Issue 模板 / CODEOWNERS**（桌面壳旋钮 `L2_REQUIRED_FOR` 可选修 → 标 P1/P2）。  
**装机 live 就绪：** 产品线 `1.3.25` · runtimeId **`1.3.25-da2adc`** · active **`preset-arina-hashimoto`** · **Startup 自启默认 OFF**（脚本会删旧 lnk）。  
**文档债：** 多处仍写「11 套内置主题」与 arina-only 产品决议冲突 → **P1 纠偏**。

---

## L0 安全底线

| 项 | 状态 | 证据 |
|----|------|------|
| 密钥不进 git | **OK** | `git ls-files` 无 `.env` / `control.token` / `*.pem` / api key；`control.token` 仅在 `%LOCALAPPDATA%\CodexDreamSkin\`（装态，未 track） |
| `.gitignore` 覆盖 env/构建物 | **弱 → 本会话补** | 已有 `node_modules/`、`dist/`、`artifacts/`、probe dumps；**原缺**显式 `.env*`（桌面仓历史无 env 面，仍应按 L0 补） |
| 漏洞上报 | **OK** | 根 `SECURITY.md`（Advisory + @xvyimu · 48h/14d）+ `docs/SECURITY.md` 威胁模型（loopback / token / schema data-only） |
| 依赖可扫 | **OK** | `pnpm-lock.yaml` + `packageManager: pnpm@11.5.0`；**生产 npm 依赖 = 0**（ADR 0004 双平面） |
| 公开 API 校验/限流意识 | **N/A→文档 OK** | 无公网 HTTP 服务；CDP/控制面 **127.0.0.1** + `x-codex-skin-token`；见 `docs/SECURITY.md` |
| asar / 危险默认 | **OK 边界** | PROJECT/PRODUCT-LAYERS 明文禁止 asar；glass 仅 probe 语义、**非** MS glass 默认开 |

**L0 结论：** PASS · 无 P0。P1：gitignore env 面硬化。

---

## L1 协作配套

| 项 | 状态 | 注 |
|----|------|-----|
| LICENSE (MIT) | **OK** | Copyright (c) 2026 xvyimu |
| NOTICE | **OK** | 独立产品线 + 宿主免责 |
| README | **OK 偏文档债** | hub + 安装/边界；**「11 套内置主题」滞后** → P1 |
| CONTRIBUTING | **OK** | 根短文 + `docs/CONTRIBUTING.md` 详规；PR 模板指 §C-1 |
| SECURITY | **OK** | 根 + docs 威胁模型 |
| CHANGELOG | **OK** | `docs/CHANGELOG.md`（Unreleased on 1.3.25） |
| CODE_OF_CONDUCT | **缺** | `COC_REQUIRED=false` → **P2** |
| .gitignore | **弱→补** | 见 L0 |
| .editorconfig | **缺** | **P2** |
| .env.example | **N/A** | 无服务端 env 契约；不硬造假 example |
| docs/PROJECT.md | **OK** | 形态与栈 SSOT |
| GITHUB_IDENTITY | **OK** | 仓名 / 安装名分离 |
| PRODUCT-LAYERS | **OK** | L0–L6 · arina-only · Startup off |

**L1 结论：** PASS。

---

## L2 OpenSSF 加强（桌面壳选修）

| 项 | 状态 | 证据 |
|----|------|------|
| CI | **OK** | `.github/workflows/themes-gate.yml`：pnpm frozen + typecheck contracts + `pnpm test`（**≠** doctor/live CDP） |
| 依赖审计门 | **弱** | CI **无** `pnpm audit`；本机 `pnpm audit` 因 **npmmirror 无 audit endpoint** 失败（exit 1）→ **P1 文档化 + Dependabot 兜底**；勿空想 CVE |
| Dependabot | **缺 → 本会话补** | 原无 `.github/dependabot.yml` |
| Issue 模板 | **缺** | **P2** |
| PR 模板 | **OK** | `.github/pull_request_template.md`（7 问 + 验收） |
| CODEOWNERS | **缺** | 单人 **P2** |
| frozen lockfile | **OK** | CI `pnpm install --frozen-lockfile` |
| branch protection | **未测** | 私有流程/单人；不阻塞 |

**L2 结论：** 选修 PASS with **P1**（Dependabot + 文档纠偏 + gitignore）；audit CI 因 registry 镜像 **DEFER** 或人 gate 换官方 registry。

---

## 装机 / live 就绪核对（本机实测 2026-07-23）

### 版本号来源（ADR 0003）

| 源 | 值 |
|----|-----|
| 产品线 `package.json` `"version"` | **1.3.25** |
| 权威 stamp | `scripts/windows/publish-runtime.ps1 -Version` → `SKIN_VERSION_TOKEN` / `versions/<id>/` |
| 装态 `current.json` | `version=1.3.25` · **`runtimeId=1.3.25-da2adc`** · `relativeEnginePath=versions/1.3.25-da2adc` · `updatedAt=2026-07-22T19:44:02Z` · `codexSkinRepo=D:\orca\Codexveil` |
| 装态 GC 残留 | 另有 `versions/1.3.25-11b868`（上一版保留） |
| 组合 tip | **`136ed99`**（docs hub）；**publish 元数据可能滞后 tip**（本机 current 指向 da2adc，非 tip 必然字节对齐） |
| `state.json` injectorPath | `…\versions\1.3.25-da2adc\scripts\injector.mjs` · injectorPid 当时存活 |

**结论：** 装机版本 SSOT = **`current.json.runtimeId`**，不是 git tip  alone。发版后须 `doctor` / `injectorPathFreshness` 再认 fresh。

### 主题 preset

| 项 | 实测 |
|----|------|
| 仓内 `themes/` | **仅** `preset-arina-hashimoto/`（arina-only 产品决议） |
| runtime 模板 `packages/runtime/assets/theme.json` | `id=preset-arina-hashimoto` |
| 装态 active-theme | **`preset-arina-hashimoto`**（桥本有菜 · rose `#E8A0BF` · surface `#1A1218`） |
| 装态 user catalog | 1 套 · 同 id |
| CLI `DEFAULT_THEME_ID` | 仍为 **`miku-488137`**（**空库/CLI 回退 id**，非装机模板默认；见 `docs/ops/visual-align-fei-away-diff-2026-07-23.md`）→ **P2** 常量改名/改值需测 |
| 文档「11 套」 | **滞后** · 与 arina-only 冲突 → **P1** 纠偏 |

**glass：** injector 内 `glass()` 为 **DOM 表面探测 helper**，非 MindSync glass 默认开；V3 矩阵明文 **forbidden glass default-on**。

### Auto-launch / Startup

| 检查 | 结果 |
|------|------|
| `install-ux-shortcuts.ps1` | **2026-07-23：Startup auto-launch disabled** — 若存在 `Codex Dream Skin - Auto Launch.lnk` 则 **删除且不重建** |
| `%APPDATA%\…\Programs\Startup` | **无** Codex/Dream/Skin lnk（仅见无关项） |
| `HKCU\…\Run` | **无** CodexDreamSkin / codex-skin 值 |
| PRODUCT-LAYERS / usage | 文档写 **默认关闭**；需自启时用户自行加 FastLaunch |

**结论：** 生产路径 **默认不写 Startup** · 与红线一致。

### 未做 / 红线遵守

| 项 | 状态 |
|----|------|
| asar 修改/重打 | **未做** |
| push | **未做** |
| glass 默认开 | **未开** |
| 密钥入库 | **未做** |

---

## 与 ChronoPortal / Chronicle 对照（文件面）

| 维度 | Chronicle | ChronoPortal | **Codexveil** |
|------|-----------|--------------|---------------|
| L0 | PASS | PASS | **PASS** |
| 形态 | Web | Web+Admin | **桌面侧车 CDP** |
| CoC / editorconfig | 后继补 | 缺 P2 | **缺 P2** |
| CHANGELOG | 后继补 | 有 | **有** |
| Dependabot | 有 | 有 | **本会话补** |
| CI audit high | 有 | 无 P1 | **镜像无 endpoint · DEFER** |
| PR 模板 | 缺 | 有 | **有** |
| 生产人闸 | SRI | CSP | **asar 禁 / Startup off** |

---

## 优先级

| Pri | 项 | MODE | 备注 |
|-----|-----|------|------|
| **P1** | `.gitignore` 加 `.env*`（及常见本地 secret 面） | docs-kit / hygiene | 本会话 |
| **P1** | README / usage / 关键入口「11 套」→ **arina-only** 纠偏 | docs-kit | 本会话；不改 runtime |
| **P1** | Dependabot（npm + github-actions） | ci-cd | 本会话 |
| P2 | CODE_OF_CONDUCT + `.editorconfig` | docs-kit | 单人不阻塞 |
| P2 | Issue 模板 / CODEOWNERS | docs-kit | 选修 |
| P2 | `DEFAULT_THEME_ID` 与 arina 对齐（需测 stamp/cli） | 小补丁 | 行为面，非本刀强改 |
| P2 | CI `pnpm audit`（需官方 registry 或自托管 advisory） | ci-cd | 本机 npmmirror 失败 |
| 另轨 | tip vs 装态 runtime 字节对齐 / 人 gate republish | publish | **不**本会话 publish |
| 另轨 | OV 代码签名 #24 | product | 非 OSS 文件面 |

---

## 本会话落地（local-commit）

| 项 | 计划 / 结果 |
|----|-------------|
| 矩阵本文 | `docs/ops/cv-oss-gap-2026-07-23.md` |
| gitignore env | **已补** `.env*` / pem / `control.token` |
| README / usage / ARCHITECTURE | **已纠偏** arina-only |
| Dependabot | **已加** `.github/dependabot.yml` |
| 测试夹具 arina-only | `theme-schema` / `dream-adapter` / `stamp` 不再依赖已删 genshin/miku 目录 |
| CoC / editorconfig | **P2 列债不阻塞** |
| 测试 | **`npm test` exit 0**（2026-07-23） |
| push / asar | **否** |

---

## 验证

```powershell
git -C C:\Users\yuanjia\orca\workspaces\Codexveil\cv-oss-ready rev-parse --short HEAD   # 基线 136ed99；补丁后 ahead
git status -sb
# 装态
Get-Content $env:LOCALAPPDATA\Programs\CodexDreamSkin\current.json
# Startup：无 Codex Dream Skin - Auto Launch.lnk
Test-Path "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\Startup\Codex Dream Skin - Auto Launch.lnk"  # expect False
```
