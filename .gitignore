# Node
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-lock.yaml.bak

# Local env / secrets (desktop product has no server .env contract; still never track)
.env
.env.*
!.env.example
*.pem
*.p12
id_rsa
id_rsa.pub
control.token

# TypeScript build outputs (contracts etc.; CI builds as needed)
*.tsbuildinfo
packages/*/dist/

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*~

# Local build / runtime scratch
_*.out
_*.err
*.log
*.bak
*.bak-*

# Product package output (Build-ProductPackage.ps1)
dist/

# Local 4-agent ship handoff (spec/changes/tests/review)
.pipeline/

# Real machine probe dumps (may contain DOM selectors / titles); keep scaffolding
docs/evidence/runs/*.json

# Never commit install-side state (lives in %LOCALAPPDATA%)
current.json.bak-*
apps/native/CodexFastLaunch/bin/
apps/native/CodexFastLaunch/obj/
artifacts/
coverage/
TestResults/

# local tool (codebase-memory) - do not commit
.cbmignore

# Firecrawl CLI scrape/crawl cache (cwd/.firecrawl); never commit site dumps
.firecrawl/
